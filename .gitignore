# Build output
/dist/
/atlasent-audit-verify
/atlasent-audit-verify.exe
*.sig
*.pem
!internal/**/*.pem
# Committed offline-verifier parity fixture (PUBLIC key only — see testdata/parity/README.md)
!testdata/parity/*.pem

# Go
*.test
*.out
